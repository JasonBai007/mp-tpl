<!--index.wxml-->
<view class="page-wrap">
  <navigator url="../detail/detail?fatherId={{item.id}}" class='item' wx:for="{{list}}" wx:key="{{index}}">
    <van-icon name="{{item.icon}}" class='inner-icon' />
    <text>{{item.title}}</text>
  </navigator>
</view>